import { atom } from "jotai";

export const isEditAtom = atom(false);
export const selectedAddressAtom = atom([]);
export const isAddAtom = atom(false);
export const stepEditAtom = atom(1);
export const addNewAddressAtom = atom(false);